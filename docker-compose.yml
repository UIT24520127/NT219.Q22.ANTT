version: '3.8'

services:
  # 1. Cơ sở dữ liệu lưu CEK và Audit Log (Vùng tối bảo mật)
  db:
    image: mariadb:10.11
    container_name: drm_mariadb
    ports:
      - "3306:3306"
    environment:
      MARIADB_ROOT_PASSWORD: root_password
      MARIADB_DATABASE: drm_system
    volumes:
      # SỬA ĐƯỜNG DẪN TẠI ĐÂY: Trỏ đúng vào thư mục mới data/database/
      - ./data/database/init.sql:/docker-entrypoint-initdb.d/init.sql:ro

  # 2. OpenBao - Quản lý khóa (KMS)
  openbao:
    image: quay.io/openbao/openbao:2.0
    container_name: drm_kms
    ports:
      - "8200:8200"
    environment:
      BAO_DEV_ROOT_TOKEN_ID: "root-token"
      VAULT_DEV_ROOT_TOKEN_ID: "root-token"
      BAO_ADDR: "http://0.0.0.0:8200"
    command: server -dev -dev-root-token-id=root-token

  # 3. Keycloak - Xác thực WebAuthn (ĐÃ ĐỒNG BỘ TỰ ĐỘNG IMPORT)
  keycloak:
    image: quay.io/keycloak/keycloak:latest
    container_name: drm_auth
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
    ports:
      - "8080:8080"
    volumes:
      # 🔥 CHIÊU THỨC QUAN TRỌNG: Gắn file realm-export.json cùng cấp vào thư mục tự động nạp của Keycloak
      - ./realm-export.json:/opt/keycloak/data/import/realm.json
    # 🔥 THAY ĐỔI LỆNH CHẠY: Bắt buộc phải có "--import-realm" để Keycloak quét thư mục import khi khởi động
    command: start-dev --import-realm

  # 4. OPA - Kiểm tra quyền hạn (Policy)
  opa:
    image: openpolicyagent/opa:latest
    container_name: drm_opa
    ports:
      - "8181:8181"
    volumes:
      # BỔ SUNG VOLUME: Mount folder policies từ gốc máy vào container
      - ./policies:/policies:ro
    command: "run --server /policies"